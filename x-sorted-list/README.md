# Sort an x-sorted List

A list L is too big to fit in memory. L is partially sorted in a specific way:

Any element is at most x indices out of position. 

L[i] < L[i+x], L[i] >= L[i-x] 

Sort the list L.
